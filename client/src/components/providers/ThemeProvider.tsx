export function ThemeProvider({ children }: { children: React.ReactNode }) {
  // ThemeProvider is implemented in @/context/ThemeContext
  // This file is kept for compatibility
  return <>{children}</>;
}
