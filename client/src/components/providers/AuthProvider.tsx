export function AuthProvider({ children }: { children: React.ReactNode }) {
  // AuthProvider is implemented in @/context/AuthContext
  // This file is kept for compatibility
  return <>{children}</>;
}
